# F15-15 W9-F6 — swallowed-error 豁免清单
#
# 由 .ci/check_swallowed_errors.sh 在 CI 阶段消费。
# 行格式： <file>:<line>:<reason>
#   - file 用正斜杠（与 check_swallowed_errors.sh 的路径归一化一致）
#   - line 数字必须与源码当前行号严格匹配；源码移动后需同步更新
#     （codex P1：该清单曾因 .sh 未接入 CI 而漂移，现已接入 lint job 强校验）
#   - reason 一句话说明，目的是 review 时能复盘是否仍合理
#
# `#` 注释 / 空行允许。
#
# 设计契约（docs/design/error-and-observability.md §3.3 末段）：
# "唯一允许豁免来源：日志写入失败（tracing / log crate 的 sink 自身错误）"，
# 实务上扩展到三类 best-effort：error-reporting sink / 脱敏近似 / 测试 teardown。

# ── 生产路径：error-reporting / 脱敏 best-effort ──
crates/cli/src/lib.rs:73:进程退出前 emit_failure 写 stderr 失败无救赎，尽力 emit + 失败不阻断 exit（§3.3 sink 失败豁免）
crates/observability/src/redact.rs:19:URL parse 失败 → 输入本就不是 URL，无需脱敏；redact 自身的 best-effort 路径属于 §3.3 末段"日志/可观测性写入失败"豁免范围
crates/observability/src/redact.rs:28:set_username 仅在 cannot-be-a-base URL 失败，本路径 URL 已验证含 authority；脱敏静默回退到 to_string 近似
crates/observability/src/redact.rs:29:set_password 同上，脱敏 best-effort，不让 log/health 崩
crates/ai/src/client.rs:230:AI error envelope JSON 解析失败 → 降级到原始 body 字符串作为 error message；不影响主流程错误传递（classify_http_status 兜底）
crates/publish/src/github.rs:221:GitHub error body 非 JSON 时降级到原始字符串；本函数只服务 error message 构造，属于 error-reporting best-effort
crates/publish/src/github.rs:511:response_message 解析 GitHub error body 取 message 字段失败时 fall-through 到原始字符串/状态码；纯 error-message 构造 best-effort

# ── 测试 harness：teardown / fire-and-forget ──
crates/config/src/loader.rs:425:测试 fixture Workspace::drop 清理 temp 目录，Drop 内无法传播错误，best-effort
crates/observability/tests/prometheus_http_test.rs:35:测试 spawn 的 hyper server 任务，退出 Err 不影响用例（端口探测已成功）；fire-and-forget
crates/observability/tests/prometheus_http_test.rs:87:同上，第二处测试 server spawn 任务
crates/observability/tests/tracing_init_file_appender_test.rs:39:测试代码：read_dir 失败时进入下一轮重试（最终断言由 deadline 兜底）
crates/storage/tests/common/pg.rs:162:PG 测试容器 cleanup，pool close 超时/失败静默吞——容器停止兜底回收
crates/storage/tests/common/pg.rs:170:PG 测试容器 cleanup，DROP SCHEMA 超时/失败静默吞——容器停止兜底回收
crates/storage/tests/common/pg.rs:184:PgTestContext::drop 兜底 cleanup 探测 tokio runtime handle，无 handle 时跳过
crates/storage/tests/common/pg.rs:190:detached 兜底 DROP SCHEMA 任务，runtime 关闭可能来不及完成，容器停止时整库回收
