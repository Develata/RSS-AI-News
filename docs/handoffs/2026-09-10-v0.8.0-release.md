# v0.8.0 发布验收

- 日期：2026-09-10
- 作者：Codex
- 分支：main
- 产品基准：`66368c9836e63a6f2d5c21c6281c83114ad82665`
- 目标 tag：`v0.8.0`
- 状态：已完成发布及读回验证

## 范围

用户授权认真验收后打 tag 并创建 GitHub Release。历史拆分已经完成；发布准备对齐 workspace/lockfile/README 的版本号、修正部署 stage 文档并冻结发布说明。

独立安全审查发现 provider 错误 JSON 的字段类型异常会绕过解码后脱敏。主代理在 AI/GitHub 两条真实 HTTP 测试上复现后修复：识别字符串 message 后脱敏，未知合法 JSON 结构不回显原文；深度超限、截断及纯文本错误保留分类并省略正文。相关测试 62 项通过，独立安全复核与 4 项边界重跑通过。同步 plan/03、04；没有新增依赖、架构层或数据库迁移。原吞错豁免仅同步 AI 行号与说明，仍为 11 条。

## 验收入口

- [发布说明与兼容事项](../reports/releases/v0.8.0.md)
- [验收矩阵](../operations/acceptance-matrix.md)
- [发布流程](../operations/ci-and-release.md)

发布前执行 candidate local matrix、独立安全审查、真实 CLI 离线管线及 exact-SHA 五项 CI；tag 后核验 GHCR manifest、版本与 GitHub Release。本地矩阵 4 lanes / 27 steps 和 release 二进制 20 步离线 CLI 均通过；原始证据摘要见 release report。PostgreSQL 与完整容器运行由 exact-SHA CI 承担，本机无 Linux Docker daemon / PostgreSQL 服务。真实 AI/GitHub 内容写入未运行。

本轮另复现了 v0.7.1 已有的 SQLite 同秒不同精度时间戳候选延迟，作为非阻断已知边界记录于 release report；未新增存储迁移。

## 发布回执

- Tag：`v0.8.0` → `ce271db49284ad02ef99ac3ca344ab9f7a6438e4`，annotated tag 已推送。
- CI `34509716462` 五项全绿；Release workflow `34510426539` 成功。
- 最终 all-features 测试 842 项通过（含 1 项子进程）、0 失败、62 忽略；单任务 Clippy 通过。
- GHCR runtime/scheduler 的全部摘要、revision、版本/帮助、scheduler 启动和 stable aliases 读回通过；digest 与执行环境限制见 release report。
- GitHub Release：https://github.com/Develata/RSS-AI-News/releases/tag/v0.8.0 。
- 本次回填仅更新发布文档；发布 tag 指向的产品代码保持冻结。
