# Rust 1.97.0 项目工具链升级

- 日期：2026-07-17
- 作者 / Agent：Codex GPT-5
- 分支：`main`
- 当前 HEAD：`f201bdc`
- 相关 commit：pending
- 相关 tag / release：N/A
- 状态：`validated`

## 工作摘要

将本地、CI、release、Docker 与 active 运维/部署文档中的 Rust exact patch pin 从 `1.94.0` 一致升级到 `1.97.0`，并在 Windows + Docker Desktop 上完成 Rust、PostgreSQL 与三类镜像验证。

## 影响范围

- crate / 模块：
  - `crates/runtime/src/flows/publish/remote.rs`
  - `.github/workflows/{ci,release}.yml`
  - `docker/Dockerfile`
- 真相源对象：
  - N/A
- 额外影响：
  - Rust toolchain pin、CI/release、Docker、active operations/plan docs

## 关键变更

### Exact toolchain pin

- `rust-toolchain.toml`、CI、release、Docker builder 与 active 文档统一使用 `1.97.0`。
- `docker/Dockerfile` builder 使用 `rust:1.97.0-slim-bookworm`。
- `docs/handoffs/` 与 `docs-backup/handoffs/` 中关于 1.94.0 的历史事实保持不变。

### Rust 1.97 Clippy 兼容

- 移除 `Iterator::zip` 第二个参数上多余的 `.into_iter()`；输入所有权和迭代语义不变。
- 未改 `Cargo.lock`，未新增 MSRV / `rust-version` 声明。

## 验证与验收

### 自动化验证

- `cargo +1.97.0 fmt --all -- --check`：通过。
- `cargo +1.97.0 clippy --workspace --all-targets --locked -- -D warnings`：通过。
- `bash .ci/check_swallowed_errors.sh`：通过，allowlist 命中 15 条。
- focused live PostgreSQL recent-entries tests：通过，2 passed / 0 failed。
- `CARGO_BUILD_JOBS=1 cargo +1.97.0 test --locked -p rss-ai-news-storage -- --include-ignored --test-threads=1`：通过，所有 test binary 0 failed。
- `CARGO_BUILD_JOBS=1 cargo +1.97.0 build --workspace --locked`：通过。
- 清除宿主继承的 `RUST_LOG=warn` 后，`CARGO_BUILD_JOBS=1 cargo +1.97.0 test --workspace --locked`：通过。该变量会过滤 file-appender 测试发出的 `info` marker；1.94 A/B 同样复现，非 1.97 回归。
- `docker pull rust:1.97.0-slim-bookworm`：通过；digest `sha256:6d220bf85c74e842a79da63997af8d2e74455c0b8847d8bb3a5888572334991d`。
- Docker `runtime` / `debug` / `scheduler` 三 target：全部 build + load 通过。

### 手工验收

- `docker run --rm rss-ai-news:runtime --help`：通过。
- `docker run --rm rss-ai-news:debug --help`：通过。
- scheduler env crontab 启动、日志、running 状态与停止：通过。
- runtime `validate-config` 缺少必需 env 时返回 exit 78：通过。

## 结果

- 本地 Windows、真实 PostgreSQL 与 Docker Desktop 验证通过，可提交并等待该 commit 的 GitHub Actions authoritative run。
- 不创建 tag 或 Release。

## 风险与后续事项

- Windows 首次并发编译曾触发 `os error 1455`（页面文件/commit 峰值）；使用 `CARGO_BUILD_JOBS=1` 后完整验证通过，未修改系统分页配置。
- 本 handoff 写入时 commit 尚未创建；最终 commit SHA 与 GitHub Actions run 以 git/remote 记录为准。

## 给下一位 Agent 的备注

- active pin 入口：`rust-toolchain.toml`、`.github/workflows/{ci,release}.yml`、`docker/Dockerfile`。
- Windows 本地重跑 file-appender 测试前检查并清除外部 `RUST_LOG=warn`，否则 `info` marker 会按设计被过滤。
