# Release v0.5.0

- tag：`v0.5.0`
- 日期：2026-06-12
- 范围：v0.4.0..v0.5.0
- 说明：本报告于 2026-07 根据 annotated tag 与 Git history 补录；不补造当时未留存的 registry/run evidence。

## 核心交付

1. **AI fallback 与凭证自治**：新增 `fallback_models`，按可回退错误分类依次尝试，并记录实际生效模型；板块级 secret 可覆盖全局 AI client。
2. **重试终态闭环**：retry budget 耗尽内折叠为 terminal state；各 flow 启动时接入 lease reclaim 与 exhausted sweep，doctor 增加对应 invariant 检查。
3. **config version 闭环**：active config row 跟随真实 `config_sha256` 轮换，存量 placeholder 首次启动自愈。
4. **PostgreSQL 收口**：补 rotate 并发实证、迁移不可变 hash gate 与 LF byte contract；storage multi-dialect 文档指向真实实现。
5. **仓库治理**：重整 plan / acceptance / map / ADR / reports / operations 文档树，并把 Rust toolchain 固定为当时的 1.94.0。

## Schema / upgrade

自动应用 migration `0003_ai_effective_model_id`：

- `article_ai_results` 新增 nullable `effective_model_id`；
- 存量数据回填；
- 配置文件无需修改；升级后首次启动可能出现一次 config `rotated` info 日志。

## 验收入口

- AI fallback：[../../plan/14-ai-fallback.md](../../plan/14-ai-fallback.md)
- retry exhaustion：[../../plan/15-retry-exhaustion.md](../../plan/15-retry-exhaustion.md)
- config versioning：[../../plan/16-config-versioning.md](../../plan/16-config-versioning.md)
- migration immutability：`crates/storage/tests/migration_immutability_tests.rs`

## Tag evidence

annotated tag 指向 `34bfd7391892fccc3d1fa0f803b730dc8c7b7796`，记录范围共 52 commits。
